[project]
name = "scene_language"
version = "0.1.0"
description = "scene_language"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kokoro-onnx>=0.6.1",
    "openai>=1.65.4",
    "pillow>=12.3.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.0.1",
    "soundfile>=0.14.0",
    "tqdm>=4.67.1",
]

[tool.setuptools]
py-modules = []
